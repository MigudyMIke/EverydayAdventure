///scr_detect_input
var_up = keyboard_check(ord("W"));
var_left = keyboard_check(ord("A"));
var_down = keyboard_check(ord("S"));
var_right = keyboard_check(ord("D"));
var_run = keyboard_check(vk_shift);
var_action = keyboard_check(vk_space);
var_pause_menu = keyboard_check(vk_escape);
var_stats = keyboard_check(vk_tab);
